---
description: Plan a new feature using the podrao-architect agent (read-only, returns a complete implementation plan)
argument-hint: <feature description>
---

Use the `podrao-architect` subagent to plan the following feature for the Podrao app:

$ARGUMENTS

The architect should produce the full 10-section plan as defined in its system prompt: feature summary, layers affected, schema changes, reuse audit, new reusable extractions, risks/trade-offs, sequence of execution, agent assignment, definition of done, and out of scope.

Wait for the architect's complete plan before doing anything else. Do NOT start implementing — present the plan to me for approval first.
