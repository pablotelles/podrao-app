/**
 * Color constants for HTML email templates.
 * Email clients do not support CSS variables — these are intentionally hardcoded here
 * as the single source of truth for email-specific colors.
 */
export const EMAIL_BRAND = '#5856d6';
export const EMAIL_ACCENT = '#E84040';
export const EMAIL_ACCENT_HOVER = '#c73232';
